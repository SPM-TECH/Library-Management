import Optionpannel from "./Optionpannel";

const OptionPanelPage = () => {
  return (
    <div className=" px-8 md:px-16 min-h-screen bg-gradient-to-b from-slate-50 to-slate-300 pt-2 dark:from-black">
      <Optionpannel />
    </div>
  );
};

export default OptionPanelPage;
