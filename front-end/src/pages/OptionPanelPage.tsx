import Layout from "../components/Layout";
import Optionpannel from "./Optionpannel";

const OptionPanelPage = () => {
  return (
    <Layout>
      <Optionpannel />
    </Layout>
  );
};

export default OptionPanelPage;
